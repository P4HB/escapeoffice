# 탈출 오피스 — 접근성·공유·재방문 구현 계획

작성일: 2026-09-22

상태: 구현 전 계획

기준: 현재 저장소의 Phaser 3 / Vite 프론트엔드, Flask / SQLAlchemy 백엔드, 규칙 버전 2

## 1. 목표와 범위

설치나 회원가입 없이 플레이를 시작하고, 성공·실패 결과를 공유하고, 친구 및 일일 도전에 참여하며, 다음 방문의 목표를 발견하는 제품을 만든다. 방문부터 공유 유입·재방문까지 측정하고, 공개 운영에 필요한 기록 신뢰도와 장애 대응을 갖춘다.

이번 계획에서는 **새로운 내부 이벤트와 게임 기믹을 제외**한다. 범위 해석의 기본값은 아래와 같다. 일반 전투 개선까지 포함하기로 범위가 바뀌면 별도 전투 계획을 추가한다.

| 이전 제안 | 이번 구현 범위 |
| --- | --- |
| 직장 풍자 이벤트·업무 사건 | 제외: 수정본 분열, 회의 소집, 긴급 업무, 새로운 스테이지 사건 |
| 전투 선택의 변화 | 제외: 신규 무기, 무기 진화·시너지, 새 보스 패턴, 적 행동·능력치 재설계 |
| 김대리 협상 확장 | 제외: 새 대화 분기, 대화로 발생하는 사건, 대화 등장 시점 변경 |
| 첫 경험 개선 | 포함: 시작 화면, 조작 안내, 목표 설명, HUD 가독성, 즉시 재도전 |
| 전투 피드백 | 포함: 기존 공격·피격·성장의 시각 반응과 효과음. 판정·수치·전투 시간은 유지 |
| 접근성·성능 | 포함: 터치 조작, 반응형 UI, 설정, 이미지 경량화, 로딩·오류 안내 |
| 결과·공유 | 포함: 성공·실패 성적표, 이미지 저장, 링크 복사, 기기 공유, 카카오톡 공유, 링크 미리보기 |
| 사회적 경쟁 | 포함: 친구 도전, 동일 조건 생성, 일일 도전, 온라인 순위와 내 주변 순위 |
| 재방문·해금 | 포함: 기존 플레이에서 달성하는 업적·목표, 칭호·외형 해금, 기록·진행도 동기화 |
| 다른 캐릭터 | 외형 선택만 포함. 캐릭터별 공격·이동·성장 능력 차이는 제외 |
| 특수 퇴근 조건 | 기존 전투 결과에 대한 업적으로 포함. 무기 사용을 강제로 제한하는 새 모드는 제외 |
| 홍보·브랜드 | 포함: HTML 소개 화면, 일관된 시각 자산, 실제 플레이 영상, 소개 자료, 채널별 유입 측정 |
| 분석·운영 | 포함: 행동 지표, 오류·성능 관측, 점수 검증, 요청 제한, AI 비용 제어, 배포·복구 |

이번 범위에 결제·광고 시스템, 실시간 멀티플레이, 길드·채팅, 사용자 제작 전투 콘텐츠, 푸시 알림은 추가하지 않는다. 기존 AI 대화는 유지하며 운영 안정성을 보강한다.

## 2. 현재 구현에서 유지할 기반과 먼저 풀 문제

| 현재 상태 | 계획에 반영할 사항 |
| --- | --- |
| `RunState`가 전투 시간과 여러 일시정지 사유를 관리 | 신규 안내·설정·터치 중단도 같은 관리 체계를 사용한다 |
| 강화·교체에 키보드와 클릭 선택이 있음 | 기존 선택 방식과 씬 종료 시 정리를 보존하고 터치를 확장한다 |
| 게스트 기록은 브라우저의 규칙 v2 TOP 10 | 원본을 보존하고 상세 기록 스키마를 별도로 만든다 |
| `GUEST_MODE`가 로그인·기록 저장·랭킹·AI를 한꺼번에 결정 | 인증 상태, 서버 연결, 대화 방식, 경쟁 기능을 분리한다 |
| 공개 배포는 GitHub Pages의 정적 게스트판 | 정식 온라인 제품은 프론트·API·공유 페이지를 같은 출처에서 제공한다 |
| 성공 화면은 시간만 전달받고 실패 화면은 종료 원인만 받음 | 종료 전에 공통 결과 객체를 확정한다 |
| 실제 플레이에 필요한 이미지 원본 합계가 약 20MB | 원본 용량과 전송량을 구분해 측정하고 경량 자산을 만든다 |
| 맵 이미지 크기가 월드 경계·시작 위치를 결정 | 맵을 축소하기 전에 논리 좌표와 이미지 해상도를 분리한다 |
| 무작위 생성에 `Math.random()`과 Phaser 난수 함수가 사용됨 | 도전 규칙의 난수와 화면 효과의 난수를 분리한다 |
| 현재 전투는 프레임 시간과 Phaser 물리 판정에 의존 | 시드만으로 재현성과 점수 검증이 완성됐다고 간주하지 않는다 |
| `/score`가 클라이언트에서 제출한 시간을 저장 | 기존 기록과 서버 검증 기록을 분리한다 |
| API 쓰기 요청은 JSON·세션·CSRF 검증을 거침 | 신규 게스트 API·분석 전송에도 보호를 유지한다 |

GitHub Pages는 정적 호스팅이므로, 개별 결과의 동적 HTML·API·서버 검증은 별도 실행 환경이 필요하다. 기존 Pages 배포는 경량 체험판과 기존 기록 내보내기 경로로 유지한다. [GitHub Pages 설명](https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages)

## 3. 완성 후 사용자 흐름

1. **첫 방문:** 공유 링크 또는 소개 화면 → 기기에 맞는 안내 → 로그인 없이 시작 → 필요한 순간에 짧은 조작 안내.
2. **한 판 종료:** 성공·실패 성적표 → 같은 모드 바로 재도전 / 결과 이미지 저장 / 공유 / 친구 도전 만들기.
3. **공유받은 방문:** 상대 결과와 도전 조건 확인 → 닉네임 선택 또는 기본 별칭 → 바로 플레이 → 상대와 결과 비교.
4. **다음 방문:** 오늘의 도전, 내 최고 기록, 달성 직전 업적, 새로 해금한 외형 확인.
5. **선택적 계정 연결:** 먼저 플레이 → 사용자가 계정을 연결하면 서버 게스트 기록·진행도 연동 → 다른 기기에서 이어서 사용.
6. **서버 장애:** 일반 플레이와 로컬 결과 보기는 가능 → 서버 검증이 필요한 도전은 연습으로 안내 → 검증되지 않은 기록을 공식 순위에 넣지 않음.

공유 링크 접근만으로 게임이나 오디오를 자동 시작하지 않는다. 게임 결과도 종료 즉시 공개하지 않고, 사용자가 공유를 선택한 경우에 공개용 결과를 만든다.

## 4. 구현 원칙과 구조 결정

### 4.1 서비스 실행 형태

| 형태 | 일반 플레이 | 개인 기록·업적 | 온라인 도전·공유 결과 | AI 대화 |
| --- | --- | --- | --- | --- |
| 정적 체험판 | 가능 | 로컬 저장 | 기본 링크·이미지 공유, 정식 사이트 안내 | 기존 규칙형 |
| 정식 사이트의 게스트 | 가능 | 로컬 + 익명 서버 프로필 | 가능, 계정 없이 참여 | 기존 규칙형을 기본값으로 사용 |
| 정식 사이트의 계정 사용자 | 가능 | 기기 간 동기화 | 가능 | 기존 온라인 모드에서 설정·예산이 있을 때 사용 |

- Phaser/Vite와 Flask/SQLAlchemy를 유지한다. 프론트 프레임워크 전체 교체는 계획에 넣지 않는다.
- 프론트 정적 자산, `/api`, `/s/...` 공유 HTML을 같은 HTTPS 출처로 배포한다. API만 다른 도메인에 두는 구성을 기본값으로 삼지 않는다.
- 개발·테스트는 SQLite를 유지한다. 운영 DB는 기존 SQLAlchemy와 호환되는 영속 DB를 사용하고, 호스팅·백업·용량을 M0에서 확정한다.
- 익명 프로필은 서버가 발급한 예측 불가능한 식별자를 세션에 연결한다. 클라이언트가 제출한 `profile_id`만으로 소유권을 판단하지 않는다.
- 기존 비밀번호 해시·HttpOnly/SameSite 세션·CSRF 보호를 유지한다. 회원가입은 플레이의 선행 조건이 아니다.
- 능력 플래그를 `onlineRecords`, `challenges`, `daily`, `kakaoShare`, `aiChat`, `analytics` 등으로 분리한다. 서버가 지원하는 기능과 인증 상태를 함께 확인한다.
- 개인별 전투 상태 복원은 구현 범위에 넣지 않는다. 동기화 대상은 완료 기록·업적·외형·설정이다.

### 4.2 기존 전투와 경쟁 기록

- 난이도 수치, 적 행동, 무기 기능, 대화 기분 공식은 이번 작업에서 기획 변경하지 않는다.
- 온라인 경쟁은 같은 규칙 프로필·난수 버전·물리 버전·대화 방식을 사용한 기록끼리 비교한다.
- 경쟁 도전에서는 현재 게스트 규칙형 대화를 사용한다. 기존 AI 대화를 사용한 일반 기록은 별도 프로필로 보관한다. 새 협상 기믹은 만들지 않는다.
- 물리 실행 방식 변경이 결과에 영향을 줄 수 있으면 새 `rules_version` 또는 `physics_version`으로 발행한다. 기존 v2 순위에 섞지 않는다.
- 화면 크기·카메라·외형·효과음 설정이 전투 판정이나 난수 흐름에 영향을 주지 않도록 한다.

## 5. 단계별 작업

모든 단계는 최종 범위에 포함된다. 단계 구분은 개발 순서와 공개 시점이며, 후반 기능을 구현 범위에서 제외한다는 뜻이 아니다. S/M/L/XL은 상대 작업량으로, 일정 약속이 아니다. 특히 M5는 가장 큰 기술 위험이 있는 단계다.

| 단계 | 결과물 | 선행 단계 | 상대 작업량 |
| --- | --- | --- | --- |
| M0 | 기준 측정·데이터 계약·기능 분리 설계 | 없음 | M |
| M1 | 빠른 시작·읽기 쉬운 UI·효과음·즉시 재도전 | M0 | M |
| M2 | PC·모바일 입력과 반응형 전체 흐름 | M0, M1 UI 구조 | L |
| M3 | 상세 결과·로컬 업적·이미지/기본 링크 공유 | M0 결과 계약, M1 | L |
| M4 | 게스트 온라인 프로필·동기화·동적 공유·분석 수집 | M0, M3 | L |
| M5 | 재현 가능한 도전·서버 검증·친구 순위 | M0, M2, M4 | XL |
| M6 | 일일 도전·업적/외형 완성·재방문 화면 | M3, M4, M5 | M–L |
| M7 | 홍보 자료·제품 실험·운영 검증·정식 공개 | 공개할 기능의 이전 단계 | M |

### M0. 기준과 데이터 계약

- [ ] **FND-01:** 현재 PC 일반 플레이, 계정 온라인 모드, 하위 경로 배포를 기준 시나리오로 기록한다.
- [ ] **FND-02:** 첫 방문 이미지 전송량, 시작 가능 시점, 실행 오류, 한 판의 전투 시간과 실제 경과 시간을 측정한다.
- [ ] **FND-03:** `RunContext`, `RunSummary`, 분석 이벤트, 기록 검증 상태를 정의한다. 아래 6절의 계약을 구현 기준으로 고정한다.
- [ ] **FND-04:** 한 판의 시작·종료를 담당하는 공통 경로와 `run_id`를 도입한다. 재시작·씬 전환으로 종료 이벤트가 중복되지 않게 한다.
- [ ] **FND-05:** 게스트 여부를 기능별 연결 상태와 분리하고, API 연결 실패 시 일반 플레이가 유지되게 한다.
- [ ] **FND-06:** 맵의 논리 크기, 객체 표시 크기, 물리 충돌 영역을 자산 픽셀 크기로부터 분리하는 규약을 만든다.
- [ ] **FND-07:** 기준 브라우저·실기기, 성능 측정 조건, 정식 출처, 운영 DB·예산을 기록한다. 운영 환경 미확정 항목은 로컬 구현을 막지 않는다.
- [ ] **FND-08:** 공식 순위의 가장 큰 위험인 재현 검증을 짧은 기술 실험으로 먼저 확인한다. 현재 충돌·회전 무기·난수 처리 중 공통 코어로 옮길 부분을 식별하고 M5의 작업량과 버전 전환 방식을 갱신한다.

**완료 기준:** 현재 정상 동작하는 일시정지·강화·교체·대화·기록 저장 시나리오가 유지되고, 신규 한 판 결과가 단 한 번 생성된다. 성능·전환율의 실측값과 목표값을 별도 열로 관리한다.

### M1. 첫 경험·로딩·피드백

- [ ] **UX-01:** 고정 2초 시작 지연을 제거한다. 자산 준비 상태와 진행 표시, 실패한 자산의 재시도를 제공한다.
- [ ] **UX-02:** 메뉴에 짧은 목표 설명과 가장 눈에 띄는 시작 버튼을 배치한다. 처음에는 플레이, 기록·설정·계정은 보조 행동으로 둔다.
- [ ] **UX-03:** 이동·경험치·강화·아이템 안내를 해당 행동이 처음 필요할 때 보여준다. 건너뛰기·다시 보기와 안내 완료 상태를 제공한다. 안내를 위해 적·무기·레벨업 수치를 바꾸지 않는다.
- [ ] **UX-04:** 실제 전투 제한 시간과 피해로 늦어지는 퇴근 시간을 시각적으로 구분한다. 피격 시 증가량, 레벨업 시 다음 선택, 아이템 잔량을 분명히 표시한다.
- [ ] **UX-05:** 성공·실패 모두 한 번의 버튼/키 입력으로 바로 `GameScene`을 새로 시작한다. 메뉴로 돌아가기는 별도 선택지로 둔다.
- [ ] **UX-06:** 기존 공격·처치·피격·레벨업·아이템 사용·성공·실패에 가벼운 시각 피드백과 효과음을 연결한다. 전투 시간을 멈추는 새 히트스톱이나 공격 판정 변경은 추가하지 않는다.
- [ ] **UX-07:** 음소거·효과음 크기·화면 흔들림 끄기·동작 줄이기·안내 다시 보기를 설정에서 제공한다. 첫 사용자 입력 이후 오디오를 활성화하며 저장된 설정을 우선한다.
- [ ] **PERF-01:** 이미지별 실제 표시 크기를 조사하고 최적화 자산·원본을 구분한다. 공유 카드에 필요한 큰 이미지도 게임 시작 시 함께 내려받지 않는다.
- [ ] **PERF-02:** 맵·스프라이트 경량화 후 월드 경계, 캐릭터 시작점, 충돌 영역, 무기 사거리·회전 반경이 유지되는지 확인한다.
- [ ] **PERF-03:** 콘텐츠 해시 기반 캐시와 엔진 청크 분리를 활용한다. 새 버전 HTML이 이전 버전 자산과 섞이지 않게 한다.

**완료 기준:** 처음 보는 이용자가 별도 설명 없이 시작·이동·강화·재도전을 수행한다. 반복 시작 10회 후 입력·오디오·화면 요소가 중복되지 않는다. 저효과 설정과 음소거로 플레이 결과가 달라지지 않는다.

### M2. 모바일·접근성

- [ ] **INPUT-01:** 키보드와 터치를 공통 `InputController`로 연결한다. 기존 방향키에 WASD를 추가하고 대각선 속도 정규화를 보존한다.
- [ ] **INPUT-02:** 가상 조이스틱과 기존 3개 아이템 슬롯 버튼을 제공한다. 이동 중 다른 손가락으로 아이템을 사용할 수 있게 한다.
- [ ] **INPUT-03:** `pointercancel`, 포커스 이탈, 화면 회전, 모달 열림/닫힘에서 눌림 상태를 해제한다. 창 복귀 시 의도치 않은 이동·아이템 사용을 방지한다.
- [ ] **LAYOUT-01:** 시작·설명·백과·계정·랭킹·결과·설정·강화·교체·대화 화면을 모두 반응형으로 만든다. 전투 화면에만 터치 버튼을 얹고 종료하지 않는다.
- [ ] **LAYOUT-02:** 세로·가로 화면에서 HUD와 조작 영역을 재배치한다. 경쟁 모드의 논리 시야 영역은 같은 비율로 유지하고 남는 공간을 UI에 사용한다. 물리 월드 크기를 기기 화면에 맞춰 변경하지 않는다.
- [ ] **LAYOUT-03:** 안전 영역, 브라우저 주소창 높이 변화, 가상 키보드 등장, 대화 입력창과 전송 버튼 가림을 처리한다.
- [ ] **A11Y-01:** 메뉴·결과·설정·폼은 접근 가능한 DOM 버튼과 레이블을 사용한다. 키보드 포커스, 닫은 모달의 포커스 복귀, 오류 읽기를 제공한다.
- [ ] **A11Y-02:** 주요 터치 목표는 최소 44 CSS px로 설계하고, 상태를 색만으로 구분하지 않는다. 작아진 캔버스 내부 글자를 단순 확대하는 방식으로 해결하지 않는다.

**완료 기준:** Android Chrome·iOS Safari 실기기에서 이동+아이템, 강화·교체, 한글 대화 입력, 종료·재시작·공유까지 완료한다. PC 회귀 검사를 함께 통과한다. 메뉴의 접근성 지원과 캔버스 실시간 전투의 지원 범위를 정확히 문서화한다.

### M3. 결과 화면·개인 기록·기본 공유

- [ ] **RESULT-01:** 성공·시간 초과·피격 실패에 공통 결과 객체를 사용한다. 전투 시간, 실제 경과 시간, 도달 레벨, 처치 수, 무기·레벨, 피격 원인별 횟수/누적 피해, 아이템 사용을 수집한다.
- [ ] **RESULT-02:** ‘퇴근 성적표’에 결과, 핵심 기록, 이번 플레이의 특징, 개인 최고 대비, 바로 재도전을 제공한다. 실패에도 의미 있는 요약을 제공한다.
- [ ] **RESULT-03:** 평가 문구는 실제 집계에 근거한다. 시간 초과를 특정 적 탓으로 표시하거나 알 수 없는 통계를 만들어내지 않는다.
- [ ] **RECORD-01:** 기존 v2 기록을 보존하고 상세 기록을 새 스키마에 저장한다. 과거 기록의 없는 통계는 ‘기록 없음’으로 표시한다.
- [ ] **RECORD-02:** 로컬 기록의 보관 상한, 저장 불가 안내, 내보내기·가져오기, 중복 방지를 구현한다. 진행 중인 플레이는 저장 오류로 중단하지 않는다.
- [ ] **SHARE-01:** 동일한 결과 데이터로 화면 성적표와 공유 이미지를 만든다. 정사각형 1080×1080과 세로 1080×1920 출력을 제공하고 긴 닉네임·한글·이모지 줄바꿈을 검증한다.
- [ ] **SHARE-02:** 이미지 저장, 기본 게임 링크 복사, 지원 기기의 이미지/텍스트 공유를 구현한다. 일반 게임 화면 캡처에만 의존하지 않는다.
- [ ] **SHARE-03:** 공유 취소·미지원·권한 거절을 정상 분기로 처리한다. 링크 복사 불가 시 선택 가능한 URL과 이미지 저장을 제공한다.
- [ ] **BRAND-01:** 이름·로고·색·글꼴·결과 도장·버튼 문구를 일관되게 사용한다. 기본 대표 이미지, 페이지 제목·설명·canonical·OG 메타데이터를 설정한다.
- [ ] **PROGRESS-01:** 기존 플레이 결과에서 첫 클리어·무피격 클리어·누적 플레이/처치·무기 사용 경험 등의 업적을 판정한다. 새로운 전투 규칙은 만들지 않는다.

**완료 기준:** 성공과 두 실패 유형 모두 PC·모바일에서 읽을 수 있는 카드를 만들고, 지원 여부와 무관하게 저장 또는 링크 공유 경로가 하나 이상 동작한다. 서버 없이도 이 단계의 일반 플레이·로컬 결과 기능을 사용할 수 있다.

Web Share는 HTTPS·사용자 동작이 필요하고 기기별 지원이 다르다. 공유 버튼을 누르기 전에 이미지를 준비해 사용자 활성화가 끊기지 않게 하며 `canShare`로 파일 공유 지원을 확인한다. Promise 완료를 메시지 발송·수신 완료로 해석하지 않는다. [Web Share API](https://developer.mozilla.org/en-US/docs/Web/API/Navigator/share)

### M4. 게스트 온라인 서비스·동적 공유·분석

- [ ] **ONLINE-01:** 익명 서버 프로필 발급, 별칭 설정, 선택적 로그인 연결을 구현한다. 계정이 없어도 일반 결과 저장과 온라인 도전에 진입할 수 있게 한다.
- [ ] **ONLINE-02:** 게스트 프로필의 서버 기록을 계정에 연결하고 기기별 중복을 처리한다. 현재 로그인은 세션을 초기화하므로, 회전 전에 서버가 확인한 게스트 소유권을 짧게 유효한 일회성 연결 증표로 보존하고 인증 후 소진한다. 다른 사람의 프로필이나 임의 클라이언트 식별자를 병합할 수 없게 한다.
- [ ] **ONLINE-03:** 로컬 기록은 출처를 표시해 가져온다. 서버 검증 기록으로 승격하지 않는다. 규칙별 최고값, 업적 집합, 외형 소유 목록을 각각의 병합 규칙으로 동기화한다.
- [ ] **ONLINE-04:** 기존 Pages 출처의 내보내기와 정식 사이트 가져오기를 제공한다. 출처가 바뀌면 기존 `localStorage`를 직접 읽을 수 없다는 점을 이전 안내에 반영한다.
- [ ] **SHARE-04:** 사용자가 선택한 결과의 공개 스냅샷과 불투명한 공유 ID를 만든다. 공개 데이터는 별칭·결과·집계·버전·선택한 외형만 허용한다.
- [ ] **SHARE-05:** `/s/{share_id}`에서 JS 없이 읽히는 결과 HTML과 개별 OG 이미지를 제공한다. 공유 미리보기와 실제 진입 화면은 같은 저장된 결과를 사용한다.
- [ ] **SHARE-06:** 결과별 공개 해제, 만료·삭제 안내, 캐시 갱신 정책을 구현한다. 외부 서비스가 이미 보관한 미리보기까지 즉시 지워진다고 안내하지 않는다.
- [ ] **SHARE-07:** 카카오톡 메시지 템플릿과 도전/플레이 링크를 연결한다. 설정되지 않은 환경에서는 기본 공유 경로를 유지한다.
- [ ] **ANALYTICS-01:** 7절의 이벤트를 수집하는 내부 수집 API와 기본 집계 화면을 만든다. 외부 분석 서비스는 필수 의존성으로 두지 않는다.
- [ ] **ANALYTICS-02:** 소량·상한 있는 재전송 큐, 중복 이벤트 ID, 서버 시간, 유입 파라미터 허용 목록, 개발/테스트 트래픽 분리를 구현한다.
- [ ] **ANALYTICS-03:** 공유 화면 호출·API 처리 완료·취소·오류·공유 링크 방문·신규 플레이를 별도 이벤트로 기록한다.
- [ ] **OPS-01:** 인터넷 공개 전에 HTTPS·세션·CSRF·요청 크기·인증/게스트 발급/공유 생성 요청 제한, 관리자 접근 제어, 로그의 민감정보 제거를 적용한다.
- [ ] **OPS-02:** DB 스키마 버전과 명시적 마이그레이션을 도입한다. `create_all`만으로 기존 테이블이 변경된다고 가정하지 않는다. 백업과 복원 절차를 검증한다.

**공개 게이트:** `aiChat`은 OPS-04의 서버 사용량·예산 제어를 완료한 환경에서만 활성화한다. 필요하면 OPS-04를 M4로 앞당기며, 미완료 상태에서는 일반 플레이의 기존 규칙형 대화를 제공한다. 기본 오류·요청 제한·백업도 M7까지 미루지 않는다.

**완료 기준:** 처음 방문한 게스트가 회원가입 없이 결과를 저장하고 공유하며, 다른 브라우저에서 그 결과를 열 수 있다. 계정 연결 후 중복 기록·진행도 손실이 없고 로그아웃·다른 계정 로그인 후 이전 계정의 비공개 기록이 노출되지 않는다. 기본 플레이는 서버 오류와 무관하게 유지된다. 자바스크립트를 실행하지 않는 요청으로도 공유 제목·설명·이미지를 확인한다.

카카오톡 공유는 SDK와 개발자 앱·등록 도메인·템플릿 설정이 필요하다. 자동 발송 기능이 아니라 사용자가 대상을 선택하는 흐름으로 구현한다. [카카오톡 공유 공식 문서](https://developers.kakao.com/docs/ko/kakaotalk-share/common)

### M5. 친구 도전·기록 검증·온라인 순위

- [ ] **CHALLENGE-01:** 일반 플레이도 시작할 때 시드와 규칙 프로필을 보관해, 종료 후 해당 조건의 친구 도전을 만들 수 있게 한다. 이전 v2 기록에서는 시드를 추정하지 않는다. AI 대화 일반 기록에서 규칙형 도전을 만들 때에는 새 조건임을 표시하고 원래 기록을 동일 조건의 목표 기록으로 사용하지 않는다.
- [ ] **CHALLENGE-02:** 도전 생성·조회·시작 API와 링크 진입 화면을 만든다. 상대 기록, 버전, 대화 방식, 검증 여부를 보여주고 바로 도전할 수 있게 한다.
- [ ] **RNG-01:** 적 종류·생성 후보·드롭·아이템 위치의 난수를 명시적으로 주입한다. 적 ID/생성 순번/드롭 용도별 흐름을 분리하고 화면 효과가 이를 소비하지 않게 한다.
- [ ] **SIM-01:** 기존 전투 로직을 렌더링과 분리한 공유 시뮬레이션 코어로 추출한다. 고정 전투 틱, 명시적인 처리 순서, 안정적인 엔티티 ID, 충돌·타겟 동률 처리 규칙을 정의한다. 좌표·각도 계산의 수치 정밀도와 양자화 규칙을 고정하고 브라우저 엔진별 차이를 검증한다.
- [ ] **SIM-02:** 렌더링 FPS와 독립적으로 입력을 전투 틱에 매핑한다. 이동, 아이템, 강화·교체 선택, 규칙형 대화의 기분 변화 근거를 재현 가능한 로그에 담는다.
- [ ] **SIM-03:** 브라우저와 서버 측 JavaScript 검증 작업이 같은 코어·규칙·난수 버전을 사용한다. Flask는 인증·저장·작업 접수, 격리된 Node 작업은 재현 검증을 담당한다.
- [ ] **VERIFY-01:** 서버가 시작 시 도전 조건, 일회성 실행 토큰, 만료 시각을 발급한다. 입력 증거는 제한된 청크로 전송하고, 종료 요청은 결과와 청크 목록·해시를 보낸다. 서버가 재현한 결과만 검증 완료로 처리한다.
- [ ] **VERIFY-02:** 청크/전체 업로드 크기·압축 해제 크기·입력 빈도·총 틱·검증 CPU/시간 상한을 둔다. 청크 순번·중복·누락·해시와 최종 수신 시각을 검증한다. 토큰 재사용, 다른 도전으로 제출, 로그 조작, 중복 완료를 처리한다.
- [ ] **VERIFY-03:** 대화 원문을 공개 결과나 분석에 저장하지 않는다. 규칙형 대화 재현에 필요한 최소 증거는 검증 전용 단기 데이터로 분리하고, 서버가 기분을 다시 계산한다. 클라이언트가 보낸 기분값 자체는 신뢰하지 않는다.
- [ ] **RANK-01:** 검증 대기·검증 완료·실패·검증 불가 상태를 분리한다. 일반·친구·일일 탭의 공식 순위는 검증 완료만 포함하고 AI 일반 기록은 개인 기록으로 유지한다.
- [ ] **RANK-02:** 규칙/물리/RNG/대화 프로필과 도전 ID가 같은 클리어 기록을 전투 틱이 적은 순으로 정렬한다. 동일 틱은 공동 순위이며 표시 순서만 안정적으로 정한다. 실패는 참여 기록과 결과 비교에 남긴다.
- [ ] **RANK-03:** 전체 상위 기록, 내 최고 기록, 내 주변 순위, 도전 참여자 비교를 제공한다. 별도 친구 주소록·친구 요청 시스템 없이 링크 단위로 경쟁한다.
- [ ] **RANK-04:** 과거 자기 신고 기록과 가져온 로컬 기록은 개인 기록에서 확인할 수 있으나 검증 순위에 넣지 않는다.

**완료 기준:** 같은 입력 로그를 브라우저/검증 작업에서 재생하면 종료 결과·전투 틱·전투 요약 해시가 같다. 해시에는 결정적인 전투 필드만 넣고 실제 경과 시간·서버 접수 시각·검증 상태는 제외한다. 30/60/120Hz 렌더링 및 지원하는 브라우저 엔진에서도 같은 로그의 결과가 같아야 한다. 적당히 그럴듯한 시간을 제출하는 것만으로 공식 순위에 진입할 수 없다. 서버가 재계산한 요약을 저장하고, 클라이언트 요약과 다르면 공식 결과를 서버값으로 확정한다.

공식 순위와 ‘검증됨’ 공유 이미지는 서버 확정 요약을 사용한다. 검증 전 공유는 참고 기록으로 표시하고, 이후 검증된 결과를 공유할 때는 새 스냅샷을 만든다. 사용자가 제출한 검증 상태나 요약값만으로 검증 배지를 발급하지 않는다.

**동일 조건의 정확한 의미:** 같은 시드·콘텐츠·규칙·난수 생성 규약을 제공한다. 기존 적 생성 위치는 플레이어 위치에 영향을 받으므로 서로 다른 이동을 한 이용자의 절대 좌표까지 같다고 보장하지 않는다. 동일 입력 로그의 재현성과 다른 입력에서도 일관된 조건 생성은 별도로 검증한다.

**검증의 한계:** 실행 토큰이나 입력 로그 자체는 실제 사람이 조작했다는 증명이 아니다. 재현 검증은 불가능한 결과 제출을 걸러내지만 봇·최적화 입력을 완전히 방지하지는 못한다. 상위 이상 기록 검토·숨김/복원·감사 로그를 운영 기능에 포함한다. 기술 검증 전에는 ‘공정한 공식 순위’로 공개하지 않는다.

### M6. 일일 도전·업적·외형·재방문

- [ ] **DAILY-01:** 서버의 Asia/Seoul 날짜를 기준으로 하루 한 개의 도전 조건을 발행한다. 기존 전투 규칙과 공통 대화 방식을 유지하고 날짜별 시드만 바꾼다.
- [ ] **DAILY-02:** 하루에 여러 번 도전할 수 있고 검증된 개인 최고만 순위에 반영한다. 첫 시도 여부와 재시도 수는 분석에서 별도로 본다.
- [ ] **DAILY-03:** 날짜 전환, 종료 직전 시작, 제출 유예, 검증 대기, 서버 시간 오차를 처리한다. 해당 날짜의 규칙 버전은 도중에 덮어쓰지 않는다.
- [ ] **DAILY-04:** 이전 날짜는 기록 열람과 연습이 가능하다. 마감 후 신규 제출을 받지 않고, 제시간에 접수된 기록의 검증이 끝나면 순위를 확정한다. 이후 운영상 무효 처리·복원만 감사 로그와 함께 허용한다. 오늘/과거/검증 중/확정 상태를 명확히 표시한다.
- [ ] **PROGRESS-02:** 첫 클리어, 무피격, 기존 무기별 사용·클리어, 누적 성취의 업적 목록과 진행도를 완성한다. 불가능한 조건이나 플레이 수치 변경을 요구하는 조건은 넣지 않는다.
- [ ] **PROGRESS-03:** 칭호, 캐릭터 외형, 결과 카드 테두리 해금을 제공한다. 이동 속도·피격 영역·공격력·경험치·아이템 수는 바꾸지 않는다.
- [ ] **PROGRESS-04:** 시작 화면에 오늘의 도전, 최근 최고 기록, 가장 가까운 업적, 새 해금을 보여준다. 진행도와 기록을 계정에 연결할 수 있게 한다.
- [ ] **PROGRESS-05:** 경쟁 외형은 동일한 논리 크기·충돌 영역을 사용한다. 희귀 외형이 상대보다 유리한 가시성이나 판정 변경을 만들지 않도록 확인한다.

**완료 기준:** 날짜 전환에도 진행 중인 도전이 다른 도전으로 바뀌지 않는다. 재접속·중복 결과 처리·기기 간 동기화가 보상을 중복 발급하지 않는다. 외형 선택 전후 동일 입력 로그의 결과가 같다.

### M7. 홍보 준비·실험·운영 완성

- [ ] **LANDING-01:** HTML 소개 화면에 게임 목표, 플레이 방식, 지원 기기, 실제 화면, 시작 버튼, 버전 소식을 제공한다. 소개를 읽지 않고 즉시 시작할 수 있게 한다.
- [ ] **BRAND-02:** 로고·아이콘·대표 이미지·공유 카드·소개 화면의 이름과 시각 체계를 통일한다. 사용한 폰트·효과음·이미지의 출처와 이용 범위를 기록한다.
- [ ] **PROMO-01:** 현재 구현된 플레이를 이용한 15–30초 영상 3종을 제작한다: 생존/성장, 빠른 퇴근 도전, 성공·실패 성적표. 제외한 기믹을 영상에 있는 것처럼 홍보하지 않는다.
- [ ] **PROMO-02:** 게임 설명, 대표 이미지, 스크린샷, 영상, 플레이 링크, 개발자 연락 수단을 담은 소개 자료를 준비한다. 채널별 링크의 캠페인 식별자를 구분한다.
- [ ] **EXPERIMENT-01:** 첫 이용자 10–15명을 대상으로 설명 없는 플레이 관찰을 수행한다. 시작·조작 이해·강화·실패 이해·재도전·공유의 문제를 기록한다.
- [ ] **EXPERIMENT-02:** 직장인 공감 이용자, 생존 게임 이용자, 방송/영상 시청자 유입을 나눠 측정한다. 초기에는 한 번에 한 가설만 바꾸고 기기·버전을 함께 비교한다.
- [ ] **EXPERIMENT-03:** 공유 클릭, 공유 링크 신규 방문, 실제 새 플레이의 연결을 평가한다. 조회수·좋아요만으로 홍보 성공을 판정하지 않는다.
- [ ] **OPS-03:** 오류율·로딩·프레임 시간·API 실패·검증 대기·DB/저장소 크기를 관측하고 알림 기준과 담당자를 정한다.
- [ ] **OPS-04:** 기존 AI 대화의 서버 실행별 횟수·기분·대화 상태, 중복 요청 방지, 시간 초과, 계정/실행/일별 사용 한도, 월 예산과 중지 스위치를 구현한다. 여러 서버에서도 동시 요청이 상한을 넘지 않도록 원자적으로 한도를 예약·정산한다. 한도 초과에도 기존 ‘보스 만나기’로 계속할 수 있게 한다. 활성화 전 완료하며 M7에서는 장애·비용 동작을 재검증한다.
- [ ] **OPS-05:** 테스트·미리보기·운영 환경과 키를 분리한다. 기능별 비활성화, 배포 되돌리기, DB 복구, 공유 공개 해제, 이상 순위 처리 절차를 작성한다.
- [ ] **OPS-06:** 정식 공개 전에 동시 접속·결과 생성·검증 부하와 장애 시나리오를 시험한다. 실제 홍보 게시·계정 설정·비용 발생 배포는 이 문서 작성과 구분된 실행 작업으로 관리한다.

**완료 기준:** 모든 홍보 링크가 해당 기능과 지원 기기로 연결되고 유입을 구분할 수 있다. API·AI·검증 작업 장애에서도 일반 플레이와 로컬 기록이 유지된다. 배포 롤백과 백업 복원을 실제 연습 환경에서 확인한다.

## 6. 데이터와 API 계약

### 6.1 공통 객체

| 객체 | 핵심 필드·불변 조건 |
| --- | --- |
| `RunContext` | `run_id`, 모드, 시드, 규칙/물리/RNG/대화 버전, 빌드 ID, 도전 ID, 시작 시각. 시작 후 조건 변경 금지 |
| `RunSummary` | 스키마 버전, 종료 상태, 전투 틱/시간, 실제 경과 시간, 레벨, 처치·피격 집계, 무기·아이템 집계. 클라이언트 제출 요약과 서버 검증 요약을 별도로 보관하고 검증 상태는 서버가 결정 |
| `InputLog` | 전투 틱별 정규화 입력, 선택 명령, 검증에 필요한 제한된 증거. 공개 결과와 분리 |
| `Profile` | 서버 익명 ID, 별칭, 선택적 계정 연결, 공개 설정. 인증·소유권은 세션에서 결정 |
| `Challenge` | 불투명 ID, 조건/시드/버전, 생성자 공개 별칭, 목표 결과, 시작·마감·제출 기한, 공개 상태 |
| `ShareSnapshot` | 원본 실행 참조, 공개 허용 필드, 이미지 템플릿 버전, 공개/해제 상태. 공유 시점의 결과 고정 |
| `Progress` | 업적 버전, 진행도, 해금 ID 집합, 장착 외형, 동기화 버전. 같은 실행으로 중복 보상 금지 |
| `AnalyticsEvent` | 이벤트 ID, 이벤트/스키마 버전, 분석 설정에 따라 사용하는 영속 익명 분석 ID, 30분 비활동 기준 세션 ID, 실행/유입 참조, 서버 수신 시각. 대화·이메일·비밀번호 수집 금지 |

새 저장소는 기존 `users`·`scores`를 바로 대체하지 않는다. `profiles`, `runs`, `challenges`, `share_snapshots`, 진행도·검증 작업·분석 관련 테이블을 마이그레이션으로 추가하고 기존 기록의 출처와 버전을 보존한다.

### 6.2 제안 API

| API | 목적 |
| --- | --- |
| `GET /api/capabilities` | 서버 기능·활성 버전·서버 시간 조회 |
| `POST /api/profiles/guest` | 익명 프로필·세션 연결, 중복 생성 방지 |
| `GET /api/me`, `POST /api/profiles/link` | 프로필 조회, 인증된 계정으로 현재 게스트 연결 |
| `POST /api/runs/start` | 실행 토큰과 확정된 조건 발급 |
| `POST /api/runs/{id}/inputs` | 입력 증거의 순번·해시를 포함한 제한된 청크 접수 |
| `POST /api/runs/{id}/complete` | 결과·청크 목록과 최종 해시 접수, 같은 요청 재시도에 같은 결과 반환 |
| `GET /api/runs/{id}` | 소유자의 저장·검증 상태 조회 |
| `POST /api/shares`, `DELETE /api/shares/{id}` | 소유한 결과 공개/공개 해제 |
| `GET /s/{id}`, `GET /s/{id}/image.png` | 공개 HTML·대표 이미지 |
| `POST /api/challenges`, `GET /api/challenges/{id}` | 친구 도전 생성·조회 |
| `GET /c/{id}` | JS 없이 읽히는 도전 소개·공통 조건·미리보기와 시작 링크 |
| `GET /api/daily`, `GET /api/leaderboards` | 일일 조건, 조건별 순위·내 주변 순위 |
| `GET /api/progress`, `POST /api/progress/sync` | 진행도 조회·멱등 동기화 |
| `POST /api/events` | 허용된 이벤트의 제한된 일괄 수집 |

신규 쓰기 API도 세션과 CSRF를 사용한다. 현재 백엔드 보호 아래에서 `sendBeacon`만 사용하는 분석 전송은 CSRF 헤더를 보낼 수 없으므로 적합하지 않다. 우선 기존 API 형태의 `fetch`와 작은 `keepalive` 요청을 사용하고, 미전송 이벤트는 상한 있는 큐로 처리한다. 브라우저 종료 전 전송을 100% 보장한다고 가정하지 않는다.

현재 요청 본문 상한은 32KB다. 300초 입력 로그를 완료 요청 하나에 넣지 않는다. JSON 헤더 여유를 둔 16KiB 이하의 인코딩된 데이터 청크를 기본값으로 하고, 전체 실행의 인코딩·압축 해제 상한은 M5 표본 로그로 확정한다. 최종 청크까지 마감 전에 접수된 경우에만 완료를 수락한다. 중단된 업로드도 만료·정리한다. 분석 배치도 32KB 미만으로 유지하며 이 기능을 위해 모든 API의 상한을 무제한으로 올리지 않는다.

### 6.3 보존·이전·시간 기본값

- 로컬 상세 기록은 최근 100판과 규칙별 최고 기록을 보관한다. 업적 집계는 별도로 유지한다. 저장 실패·초과 시 기존 기록을 무조건 지우지 않는다.
- 기존 v1/v2 기록은 원본 키를 보존한다. 내보내기 파일에는 스키마/출처/버전을 넣고 가져오기 크기와 필드를 검증한다.
- 출처가 다른 기존 Pages 기록은 명시적 내보내기·가져오기로 이동한다. 읽을 수 없는 옛 기록을 자동 이전 완료로 표시하지 않는다.
- 초기 보존 정책은 원시 분석 30일, 입력 검증 자료 7일, 집계 180일을 제안한다. 공개 기록·계정 삭제와 이의 검토에 필요한 보존 범위는 정식 공개 전에 운영 문서로 확정한다.
- 일일 도전 시작 가능 시간은 KST 00:00부터 다음 날 00:00 직전까지다. 등록 후 실제 경과 시간은 최대 30분, 전날 도전 제출은 다음 날 00:30까지 허용하는 것을 기본값으로 한다. 전투 제한 300초는 유지한다.
- 제출 마감 전에 접수된 검증 대기 기록은 검증 완료 후 순위에 반영한다. 서버 접수 시각이 기준이며 기기 시계 변경은 기한을 연장하지 않는다.
- 친구 도전 링크는 기본 30일 동안 새 기록을 받을 수 있게 하고 이후 결과 열람·연습은 유지한다. 버전 지원 종료 시 같은 조건을 조용히 다른 규칙으로 바꾸지 않고 종료 안내를 제공한다.

## 7. 분석 이벤트와 성공 판단

### 7.1 필수 이벤트

| 구간 | 이벤트 | 중복·해석 기준 |
| --- | --- | --- |
| 유입·준비 | `visit`, `menu_ready`, `load_error` | 유입 경로는 허용 목록으로 정규화, 기기·빌드 구분 |
| 시작·학습 | `run_started`, `tutorial_step`, `first_upgrade`, `active_60s` | 각 실행/단계에서 한 번만 기록 |
| 핵심 진행 | `dialogue_opened`, `boss_reached` | 대화 원문·입력 내용은 분석에서 제외 |
| 종료·재도전 | `run_ended`, `result_viewed`, `retry_started` | 종료와 결과 열람 구분, 이전 실행 ID로 같은 세션 재도전 연결 |
| 오류·중단 | `run_error`, `run_interrupted` | 오류 종류·실행/빌드 ID만 수집. 강제 종료 등 관측 못한 중단은 미종료로 분리 |
| 공유 | `share_intent`, `share_api_resolved`, `share_cancelled`, `share_failed`, `image_exported`, `link_copied` | API 완료를 실제 메시지 발송 완료로 치환하지 않음 |
| 공유 유입 | `shared_landing_view`, `referred_run_started` | 크롤러 요청은 플레이로 계산하지 않음, 링크 방문과 시작 구분 |
| 경쟁·성장 | `challenge_started`, `daily_started`, `run_verified`, `achievement_unlocked`, `profile_linked` | 검증 성공은 서버에서 발행 |

### 7.2 지표 정의

- **시작 전환율:** 시작한 신규 방문자 / 플레이 지원 기기의 신규 방문자. 지원하지 못한 기기와 로딩 실패도 별도 전체 퍼널에 남긴다.
- **첫 강화 도달률:** 첫 실행에서 강화를 경험한 사용자 / 첫 실행을 시작한 사용자.
- **재도전율:** 첫 결과를 본 뒤 같은 세션에서 재도전한 사용자 / 첫 결과를 본 사용자. 세션은 30분 비활동 시 새로 구분한다.
- **공유 의향 행동률:** 결과에서 공유·복사·이미지 저장을 시도한 사용자 / 결과를 본 사용자. 실제 발송률과 이름을 구분한다.
- **공유 유입 전환율:** 공유 링크 방문 후 플레이한 신규 사용자 / 공유 링크로 방문한 신규 사용자.
- **관측 가능한 추천 기여:** 특정 기간 공유 링크를 통해 시작한 신규 사용자 / 같은 기간의 기존 활성 사용자. 측정되지 않는 메신저 전달까지 포함한 전체 바이럴 계수라고 부르지 않는다.
- **D1/D7 재방문:** 첫 플레이 날짜 기준으로 정확히 다음 날/7일 후 다시 플레이한 사용자 비율. KST 날짜·동일 코호트 정의를 사용하고 집계 기간이 끝난 코호트만 비교한다.
- **기술 지표:** 방문→메뉴 준비, 시작 클릭→조작 가능, 전투 프레임 시간, 오류 없이 종료된 실행 비율, API 실패율, 검증 지연.

재방문 분석은 분석 수집을 허용한 사용자의 영속 익명 분석 ID로 계산하고 세션 ID와 혼용하지 않는다. 쿠키/저장 공간 초기화·기기 변경으로 익명 사용자가 중복 집계될 수 있음을 대시보드에 표시한다. 수집 비허용 사용자는 개인 코호트 분모에서 제외하며 전체 이용자를 대표한다고 주장하지 않는다. 외부 분석 전송 없이도 기능은 동작하고, 분석 수집 사용 여부·보존 범위에 대한 안내와 설정을 제공한다.

전환율·재방문율은 처음부터 임의의 업계 합격선으로 정하지 않는다. M0/M7에서 기기·채널별 기준값을 얻고 변경 전후를 비교한다. 초기 10–15명 관찰은 사용성 문제 발견용이며 바이럴 성과의 통계적 증명이 아니다.

## 8. 품질 목표와 검증

아래 수치는 초기 엔지니어링 목표다. 현재 달성한 값이 아니며 M0에서 기준 기기와 측정 조건을 고정한 뒤 조정 사유를 기록한다.

| 항목 | 초기 목표·검증 조건 |
| --- | --- |
| 첫 플레이 자산 | 실제 압축 전송량 합계 5MB 이하를 목표로 설정. 별도 원본 파일 용량도 기록 |
| 시작 속도 | 콜드 캐시·10Mbps·왕복 지연 150ms에서 첫 메뉴 3초, 첫 조작 가능 6초 이내를 목표. 광고성 소개 페이지 표시를 게임 준비로 집계하지 않음 |
| 전투 성능 | 기준 모바일에서 60fps 목표, 적 100마리 시 프레임 시간 p95 33ms 이하. PC·모바일 실측 결과 분리 |
| 반복 실행 | 종료→재시작 10회에 입력·DOM·오디오·타이머 중복 없음, 메모리의 지속 증가 없음 |
| 입력 | 이동+아이템 멀티터치, 화면 회전, 앱 전환·복귀, 대화 입력, 모달 종료 후 조작 복구 |
| 결과·저장 | 성공/실패 집계 정확성, 중복 종료 방지, 저장 불가·기록 손상·이전 파일 검증 |
| 공유 | PC/모바일 저장·복사·공유 취소·미지원, 긴 별칭, 공개 해제, JS 없는 OG 조회 |
| 경쟁 | 동일 로그 재현, 다른 FPS, 난수 흐름 독립성, 위조 제출, 만료·재사용 토큰, 검증 지연 |
| 일일 도전 | KST 자정, 제출 유예, 기기 시계 오차, 중복 보상, 검증 완료가 늦은 기록 |
| 배포 | 루트·하위 경로, 자산 캐시 갱신, 서버 없는 정적 체험판, DB 이전·복구 |

자동화는 위험이 큰 계약과 회귀 동작에 집중한다. 단순 문구·색상 변경에 구현을 그대로 따라 쓰는 테스트를 추가하지 않는다.

- 프론트 단위 검사: 기존 `npm test`에 결과 집계·기록 이전·입력 정규화·도전 조건·업적·이벤트 중복 방지를 추가한다.
- 브라우저 검사: 기존 `npm run test:browser`에 첫 방문·직접 재시작·터치·공유 대체 경로·계정 연결·서버 오류를 확장한다.
- 백엔드 검사: 기존 unittest에 익명 프로필 소유권·CSRF·마이그레이션·멱등 완료·공개 해제·일일 마감을 추가한다.
- 검증 코어: 고정된 입력 로그 모음으로 버전별 동일 결과와 조작 거절을 확인한다. 물리 이식 이후에는 실제 밸런스 실험과 사람 플레이로 기존 체감의 변화도 점검한다.
- 실기기 검사: Chromium 에뮬레이션으로 iOS Safari 검증을 대체하지 않는다. 공유·오디오·주소창·가상 키보드는 실제 지원 기기에서 확인한다.
- CI: 필수 검사 성공 후 동일 빌드 산출물을 배포한다. 검증 워크플로와 별개로 먼저 배포되어 버리는 구조를 없앤다.

## 9. 주요 파일과 신규 모듈 배치

아래 신규 파일명은 제안이며, 구현 과정에서 책임이 명확하면 조정할 수 있다.

| 영역 | 기존 수정 지점 | 신규 책임/제안 위치 |
| --- | --- | --- |
| 시작·모드 | `frontend/main.js`, `scenes/BootScene.js`, `services/gameMode.js` | `services/capabilities.js`, 시작·설정 DOM UI |
| 한 판·결과 | `scenes/GameScene.js`, `ClearScene.js`, `GameOverScene.js`, `services/runState.js` | `services/runSummary.js`, `runHistory.js`, 공통 결과 UI |
| 입력·레이아웃 | `objects/Player.js`, `ui/ChoiceModal.js`, `frontend/index.html` | `input/InputController.js`, `ui/TouchControls.js`, `ui/layout.js` |
| 효과·설정 | `GameScene.js`, 기존 객체·선택 UI | `services/settings.js`, `effects/Feedback.js`, `services/audio.js` |
| 공유 | 결과 UI, `frontend/index.html` | `services/share.js`, 결과 이미지 템플릿, `backend/routes/share_routes.py` |
| 경쟁 코어 | `objects/*`, `systems/monsterspawn.js`, `config/balance.js` | `shared/simulation/`, `shared/contracts/`, `verifier/` |
| 온라인 프로필 | `services/api.js`, 계정 화면, `backend/routes/user_routes.py` | 프로필·실행·도전·진행도 라우트와 모델 |
| 분석 | 시작·선택·종료·공유 호출 지점 | `services/analytics.js`, `backend/routes/event_routes.py`, 집계 작업·관리 화면 |
| 이전·운영 | `backend/db_config.py`, `app.py`, `.github/workflows/*` | 명시적 DB migrations, 모니터링·보존·검증 작업 운영 설정 |

`GameScene`이 공유·분석·오디오·저장을 모두 직접 구현하지 않게 한다. 씬은 한 판의 사건을 전달하고 전용 모듈이 처리하며, 네트워크 완료를 기다리는 동안 전투 루프를 막지 않는다.

## 10. 병렬 작업과 통합 순서

- M0에서 결과·입력·모드 계약을 고정한 뒤 M1 성능/시각 작업, M2 모바일, M3 결과 UI를 분리해 진행할 수 있다.
- M4 백엔드 API와 M3 공유 UI는 계약·가짜 응답을 공유해 병렬 개발할 수 있다.
- M5 시뮬레이션 추출은 `GameScene`·객체·난수 코드를 크게 건드리므로 담당을 한 작업 흐름으로 모으고 짧은 단계로 통합한다.
- M6 업적 화면·외형 자산, M7 소개 자료는 전투 코어 변경과 독립적으로 준비할 수 있다.
- 신규 작업은 기존 작업자의 변경을 되돌리지 않고, 계약→코어→UI 연결→회귀 검사의 작은 단위로 반영한다.

## 11. 주요 위험과 대응

| 위험 | 대응·완료 판단 |
| --- | --- |
| 자산 축소로 맵·충돌 범위 변경 | 논리 월드와 충돌 영역 고정 후 최적화, 기준 장면 비교 |
| 터치와 화면 비율이 경쟁상 이점 제공 | 공통 이동 규칙·시야 영역·입력 틱, 기기별 성능 기록 |
| 시드만 같고 실제 결과는 불일치 | 난수 흐름 분리 + 공통 고정 틱 코어 + 재현 검증 |
| 서버 검증이 너무 비쌈 | 제한된 로그·틱·CPU, 작업 큐, 동시 처리 상한, 검증 대기 UI |
| 기존 기록이 새 순위의 신뢰도 저해 | 버전·출처·검증 상태 분리, 기존 기록 보존 |
| 출처 변경으로 기록이 사라진 것처럼 보임 | 이전 Pages에서 내보내기 제공 후 정식 사이트 가져오기 |
| 데이터 수집이 플레이를 지연시킴 | 비동기 소량 큐·상한·수집 중지 기능, 수집 실패와 플레이 분리 |
| 사용자별 OG가 정적 배포에서 나오지 않음 | 동일 출처의 서버 HTML/이미지, 기본 체험판은 기본 OG로 제한 |
| 공유·오디오가 특정 기기에서 실패 | 기능 확인과 대체 경로, 실기기에서 최종 확인 |
| AI 무료 호출·반복 요청으로 비용 증가 | 서버 실행별 횟수·일별/월별 한도·중복 방지·즉시 중지 |
| 구현 범위에 새 기믹이 다시 섞임 | 각 작업이 접근성·표현·공유·경쟁·진행도 중 어디에 속하는지 검토 |

## 12. 공개 전 확정할 항목과 최종 완료 기준

### 필요한 외부 준비

| 항목 | 계획의 기본값 | 확정 시점 |
| --- | --- | --- |
| 정식 도메인·호스팅 | 프론트/API/공유 페이지 동일 출처, 기존 Pages 유지 | M4 인터넷 공개 전 |
| 운영 DB·저장소·검증 작업 예산 | 기존 스택을 유지하고 사용량 상한을 설정 | M4/M5 공개 전 |
| 카카오 개발자 앱·웹 도메인·템플릿 | 기본 공유는 독립 동작, 카카오 기능은 설정 후 활성화 | SHARE-07 실제 검증 전 |
| 브랜드 자산·효과음 | 기존 콘셉트를 통일하고 이용 가능한 자산만 배포 | M1/M3/M7 자산 확정 시 |
| 실기기·테스트 참여자 | PC·Android·iOS, 초회 관찰 10–15명 | M2/M7 |
| 운영 담당·원시 데이터 보존·비용 상한 | 문서의 제안값을 운영 정책으로 확정 | 정식 공개 전 |

### 최종 체크리스트

- [ ] 첫 방문부터 플레이·종료·재도전까지 PC와 모바일에서 계정 없이 완료된다.
- [ ] 기존 전투 규칙을 유지하며 안내·화면·효과음·로딩이 개선된다.
- [ ] 성공과 실패 결과 모두 저장·공유할 수 있고, 공유받은 이용자가 바로 플레이한다.
- [ ] 친구 도전·일일 도전의 조건이 재현되고 검증된 기록만 공식 순위에 반영된다.
- [ ] 기존 v2 기록과 새 상세 기록이 공존하며 출처 변경 시 이전 경로가 제공된다.
- [ ] 업적·외형 해금·기록·진행도가 제공되고 선택적 계정 연결로 동기화된다.
- [ ] 방문·첫 경험·재도전·공유 유입·D1/D7 지표를 정의한 분모와 함께 볼 수 있다.
- [ ] 기본 소개·브랜드·영상·채널별 링크와 실사용자 관찰 결과가 준비된다.
- [ ] 오류·요청 제한·점수 검증·AI 비용·DB 백업·복원·롤백이 시험된다.
- [ ] 제외한 신규 이벤트·보스 패턴·무기 진화·대화 분기·캐릭터 능력이 추가되지 않았다.

최종 구현 완료는 위 체크리스트 충족을 의미한다. 바이럴과 인지도 상승은 보장되는 결과가 아니며, 공개 후 측정한 이용자 행동으로 효과를 판단한다.
