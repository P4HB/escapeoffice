FLASK_SECRET_KEY=
# Optional: mysql+pymysql://APP_USER:PASSWORD@127.0.0.1/escape_db
# Default is backend/escapeoffice.db (SQLite).
# DATABASE_URL=
# false is for local HTTP development only; use true with HTTPS.
SESSION_COOKIE_SECURE=false
# Both values are required only for online AI dialogue.
GEMINI_API_KEY=
GEMINI_MODEL=
