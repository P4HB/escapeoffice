{
  "name": "escapeoffice",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:browser": "playwright test",
    "test:balance": "node tools/simulate-balance.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
