# Default: serverless guest edition. Set false only with the account/chat APIs deployed.
VITE_GUEST_MODE=true
# Set /escapeoffice/ for a local build matching the GitHub Pages project URL.
VITE_BASE_PATH=/
